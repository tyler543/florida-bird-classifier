# For basic functionality with cpu
torch==2.10.0
torchvision==0.25.0
birder==0.4.1
# For GPU support (comment out if using CPU)
# pip install torch==2.7.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
